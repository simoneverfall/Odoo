<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--    Form View customizations -->
    <record id="sale_order_inherited_form_everfall" model="ir.ui.view">
        <field name="name">sale.order.inherited.form.everfall</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <!-- OLD LOGIC!!!! -->
            <!-- <xpath expr="//field[@name='partner_shipping_id']" position="attributes">
                <attribute name="context">{'show_address': 1}</attribute>
            </xpath>

            
            <xpath expr="//field[@name='partner_invoice_id']" position="attributes">
                <attribute name="string">Provider</attribute>
            </xpath>
 -->

            <!-- OLD LOGIC!!!! -->

            <xpath expr="//field[@name='validity_date']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="project_id" readonly="1"/>
                <field name="x_state_custom"/>
            </xpath>

            <!-- Previous Logic  -->
            <!-- <xpath expr="//field[@name='partner_id']" position="replace">
                <field name="partner_id"
                       string="Case Manager"
                       widget="many2one"
                       context="{'res_partner_search_mode': 'customer', 'show_address': 1, 'show_vat': True, 'only_name': True}"
                       placeholder="Type to find a customer..."
                       readonly="state in ['cancel', 'sale']"/> -->
            <!-- Now Making This As Client Delivery Address CHANGED!!!!!-->
            <!-- IT MUST BE CASE MANAGER-->
            <xpath expr="//field[@name='partner_id']" position="replace">
                <field name="partner_id"
                       string="Case Manager"
                       widget="many2one"
                       context="{'res_partner_search_mode': 'customer', 'show_address': 1, 'show_vat': True, 'only_name': True}"
                       placeholder="Type to find a case manager..."
                       readonly="state in ['cancel', 'sale']"/>
                <!-- context="{'res_partner_search_mode': 'customer', 'show_address': 1, 'show_vat': True, 'only_name': True}" -->

            </xpath>

            <xpath expr="//field[@name='partner_shipping_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            
            <xpath expr="//field[@name='partner_invoice_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

            
            <xpath expr="//notebook" position="inside">
                <page string="Client Photos">
                    <field name="client_photo_ids" widget="many2many_binary" string="Attach a Photo"/>
                </page>
            </xpath>

            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="client_id" string="Client Details" options="{'no_create':1}" context="{'res_partner_search_mode': 'customer', 'show_address': 1, 'show_vat': True, 'only_name': True}" readonly="state in ('sale','cancel')"/>
                <field name="provider_id" readonly="invoice_status == 'invoiced'"/>
                <!-- <field name="case_manager_id" readonly="state == 'cancel' or locked" context="{'show_address': 1}"/> -->
            </xpath>

            <xpath expr="//form//div[@class='oe_button_box']" position="inside">

                

                <button type="object" name="action_open_project" class="oe_stat_button" icon="fa-puzzle-piece" invisible="not project_id">
                    <field name="custom_project_count" widget="statinfo" string="Project"/>
                </button>

                <button type="object" name="action_open_task_calendar" class="oe_stat_button" icon="fa-check" invisible="not task_id">
                    <field name="custom_task_count" widget="statinfo" string="Task"/>
                </button>
            </xpath>

            
        </field>
    </record>

    <record id="custom_sale_order_project_task_view_inherit" model="ir.ui.view">
        <field name="name">custom.sale.order.project.task.view.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_project.view_order_form_inherit_sale_project"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_view_project_ids']" position="replace">
                <button type="object" name="action_view_project_ids" class="oe_stat_button" icon="fa-puzzle-piece" invisible="1" groups="project.group_project_user">
                    <field name="project_count" widget="statinfo" string="Projects"/>
                </button>
            </xpath>

            <xpath expr="//button[@name='action_view_task']" position="replace">
                <button type="object" name="action_view_task" class="oe_stat_button" icon="fa-check" invisible="1" groups="project.group_project_user">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Tasks</span>
                        <span class="o_stat_value">
                            <field name="closed_task_count"/> / <field name="tasks_count"/>
                            (<field name="completed_task_percentage" widget="percentage" options="{'digits': [1, 0]}"/>)
                        </span>
                    </div>
                </button>
            </xpath>
        </field>
    </record>

    <record id="custom_sale_order_sale_timesheet_view_order_form_inherit" model="ir.ui.view">
        <field name="name">custom.sale.order.sale.timesheet.view.order.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale_timesheet.view_order_form_inherit_sale_timesheet"/>
        <field name="arch" type="xml">
            
            <xpath expr="//button[@name='action_view_timesheet']" position="replace">
                <button type="object"
                           name="action_view_timesheet"
                           class="oe_stat_button"
                           icon="fa-clock-o"
                           invisible="1"
                           groups="hr_timesheet.group_hr_timesheet_user">
                           <div class="o_stat_info">
                               <span class="o_stat_value">
                                    <field name="timesheet_total_duration" class="mr4"/>
                                    <field name="timesheet_encode_uom_id" options="{'no_open': True}"/>
                                </span>
                                <span class="o_stat_text">Recorded</span>
                           </div>
                        </button>
            </xpath>

        </field>
    </record>

    

    <!--    list View customizations -->
    <record id="sale_order_inherited_list_everfall" model="ir.ui.view">
        <field name="name">sale.order.inherited.list.everfall</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree_with_onboarding"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total']" position="after">
                <field name="x_state_custom" widget="badge" decoration-info="x_state_custom == 'booked' or x_state_custom == 'approved'" decoration-warning="x_state_custom == 'hold'" decoration-primary="x_state_custom == 'awaiting_approval' or x_state_custom == 'no_quote_required'" decoration-danger="x_state_custom == 'cancelled'" decoration-success="x_state_custom == 'complete'"/>
            </xpath>
            <xpath expr="//field[@name='validity_date']" position="replace">
            </xpath>
<!--            <xpath expr="//field[@name='create_date']" position="before">-->
<!--            </xpath>-->

            <xpath expr="//field[@name='state']" position="after">
<!--                <field name="task_id" />-->
                     <button name="action_open_task_calendar"
                        string="View Task📅"
                        invisible="not task_id"
                    type="object"
                    class="btn-link"
                />
            </xpath>
            <xpath expr="//field[@name='create_date']" position="before">
                <field name="client_name" string="Client Name"/>
                <field name="details" string="Address"/>
                <field name="suburb"/>

<!--                attrs="{'invisible': [('task_id','=',False)]}"-->

                <!--                <field name="provider_name"/>-->
            </xpath>
            <!-- OLD LOGIC -->
            <!-- <xpath expr="//field[@name='partner_id']" position="attributes">
                <attribute name="context">{'only_company': True}</attribute>
                <attribute name="string">Case Manager</attribute>
            </xpath> -->

            <!-- new logic -->
            <!-- <xpath expr="//field[@name='partner_id']" position="attributes">
                <attribute name="optional">hide</attribute>
                <attribute name="string">Client Company</attribute>
            </xpath> -->

            <!-- fixed as partner_id is case manager!!! -->
            <xpath expr="//field[@name='partner_id']" position="attributes">
                <attribute name="string">Case Manager</attribute>
            </xpath>

            <!-- OLD Logic -->

            <!-- <xpath expr="//field[@name='partner_id']" position="before">
                <field name="partner_invoice_id" string="Provider" context="{'only_company': True}"/>
            </xpath> -->

            <!-- New Logic -->
             <xpath expr="//field[@name='partner_id']" position="before">
                <field name="provider_id"/>
             </xpath>

             <!-- New Logic -->
        </field>
    </record>

    <record id="sale_order_custom_search_view" model="ir.ui.view">
        <field name="name">sale.order.custom.search.view</field>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <search>
                <field name="client_name" string="Client Name"/>
                <field name="details" string="Address"/>

                <filter string="WIP" name="wip" domain="[('state', '!=', 'cancel'),('x_state_custom', 'not in', ('complete','hold','cancelled'))]"/>
                <separator/>
                <filter string="My Quotations" name="my_quotation" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Quotations" name="draft" domain="[('state', 'in', ('draft', 'sent'))]"/>
                <filter string="Sales Orders" name="sales" domain="[('state', '=', 'sale')]"/>
                <separator/>
                <filter string="Complete" name="complete" domain="[('x_state_custom', '=', 'complete')]"/>
                <filter string="Approved" name="approved" domain="[('x_state_custom', '=', 'approved')]"/>
                <filter string="Awaiting Approval" name="awaiting_approval" domain="[('x_state_custom', '=', 'awaiting_approval')]"/>
                <filter string="Booked" name="booked" domain="[('x_state_custom', '=', 'booked')]"/>
                <filter string="Cancelled" name="cancelled" domain="[('x_state_custom', '=', 'cancelled')]"/>
                <filter string="Hold" name="hold" domain="[('x_state_custom', '=', 'hold')]"/>
                <separator/>
                <filter string="Create Date" name="filter_create_date" date="create_date"/>
                <filter string="My Orders" domain="[('user_id', '=', uid)]" name="my_sale_orders_filter"/>
                <filter invisible="1" string="Late Activities" name="activities_overdue"
                    domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                    help="Show all records which has next action date is before today"/>
                <filter invisible="1" string="Today Activities" name="activities_today"
                    domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                    domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Group By">
                    <filter string="Salesperson" name="salesperson" domain="[]" context="{'group_by': 'user_id'}"/>
                    <filter name="customer" string="Customer" domain="[]" context="{'group_by': 'partner_id'}"/>
                    <filter string="Order Date" name="order_month" domain="[]" context="{'group_by': 'date_order'}"/>
                    <filter string="Suburb" name="suburb" domain="[]" context="{'group_by': 'suburb'}"/>
                </group>

            </search>
        </field>
    </record>

    <record id="sale.action_quotations_with_onboarding" model="ir.actions.act_window">
        <field name="search_view_id" ref="custom_sale_order.sale_order_custom_search_view"/>
        <field name="context">{'search_default_my_quotation': 1, 'no_project_create': 1}</field>
    </record>
    <record id="sale.action_orders" model="ir.actions.act_window">
        <field name="search_view_id" ref="custom_sale_order.sale_order_custom_search_view"/>
        <field name="context">{'no_project_create': 1}</field>
    </record>
</odoo>